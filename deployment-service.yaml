apiVersion: v1
kind: Namespace
metadata:
  name: auth
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: socket-wireguard
  namespace: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: socket-wireguard
  template:
    metadata:
      labels:
        app: socket-wireguard
    spec:
      containers:
        - name: socket-wireguard
          image: socket-wireguard:latest
          ports:
            - name: http-port
              containerPort: 8000
            - name: wireguard-port
              containerPort: 51820
              protocol: UDP
          env:
            - name: ISKUBERNETES
              value: "false"
            - name: ADDRESS
              value: "10.0.0.0"
            - name: PORT
              value: "8000"
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: redis-url-config
                  key: redis-url
          volumeMounts:
            - name: wireguard-config
              mountPath: /etc/wireguard
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
      volumes:
        - name: wireguard-config
          hostPath:
            path: /etc/wireguard  # Absolute path for hostPath
            type: Directory
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-new
  namespace: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-new
  template:
    metadata:
      labels:
        app: redis-new
    spec:
      containers:
        - name: redis
          image: redis:latest
          ports:
            - name: redis-port
              containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: socket-wireguard-service
  namespace: auth
spec:
  type: LoadBalancer  # Assuming cloud provider with LoadBalancer support
  ports:
    - name: http-port
      port: 8000
      targetPort: 8000
    - name: wireguard-port
      port: 51820
      targetPort: 51820
      protocol: UDP
  selector:
    app: socket-wireguard
---
apiVersion: v1
kind: Service
metadata:
  name: redis-new-service
  namespace: auth
spec:
  ports:
    - name: redis-port
      port: 6379
      targetPort: 6379
  selector:
    app: redis-new
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-url-config
  namespace: auth
data:
  redis-url: "redis://redis-new-service.auth.svc.cluster.local:6379"  # Redis service URL for WireGuard
  



